package me.uport.sdk.transport

import kotlinx.coroutines.experimental.runBlocking
import org.junit.Assert.*
import org.junit.Test

class IntentSenderTest {

    @Test
    fun `setup is ok`() = runBlocking {
        assertTrue(true)
    }

}